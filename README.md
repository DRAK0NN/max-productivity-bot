# ProdMax Bot 🤖

Чат-бот для мессенджера MAX, помогающий молодежной аудитории (18-25 лет) повысить продуктивность через тайм-менеджмент, трекинг привычек и карьерное планирование.

## 🎯 Возможности

- **📝 Управление задачами** - создание, отслеживание и завершение задач
- **🍅 Pomodoro таймер** - классический таймер продуктивности (25 минут)
- **✅ Трекер привычек** - ежедневные привычки со streak'ами и наградами
- **🚀 Карьерные цели** - планирование и отслеживание прогресса по целям
- **📊 Статистика и XP** - система уровней и опыта за выполнение задач
- **💡 Рекомендации** - персональные советы на основе прогресса

## 🛠 Технологии

- **Backend**: Node.js + Express
- **Database**: PostgreSQL (Supabase)
- **API**: MAX Bot API
- **Containerization**: Docker

## 📋 Требования

- Node.js >= 18.0.0
- Docker и Docker Compose (для контейнеризации)
- PostgreSQL (или Supabase аккаунт)
- Токен бота MAX

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонируй репозиторий
git clone https://github.com/DRAK0NN/max-productivity-bot
cd MAX

# Установи зависимости
npm install
```

### 2. Настройка переменных окружения

Создай файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```


### 3. Запуск через Docker (рекомендуется)

```bash
# Сборка и запуск всех сервисов (БД + бот)
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

### 4. Запуск локально (без Docker)

```bash
# Убедись, что PostgreSQL запущен
# Создай БД:
createdb prodmax

# Запусти миграции (автоматически при старте)
npm start

# Или в режиме разработки с автоперезагрузкой:
npm run dev
```

### 5. Настройка Webhook

Бот должен получать события от MAX API через webhook. Для локальной разработки используй туннель:

#### Вариант 1: ngrok (если доступен)

```bash
# Установи ngrok: https://ngrok.com/download

# Запусти туннель
ngrok http 3000

# Скопируй URL (например: https://abc123.ngrok.io)
# Установи webhook:
curl -X POST http://localhost:3000/setup-webhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://abc123.ngrok.io/webhook"}'
```

#### Вариант 2: clo (альтернатива ngrok, если ngrok недоступен)

```bash
# Установи clo (если еще не установлен)
# Обычно это cloudflared или похожий инструмент
# npm install -g @cloudflare/cloudflared
# или используй готовый бинарник

# Запусти туннель (Linux)
./clo publish http localhost:3000

# Запусти туннель (Windows)
cloudflared tunnel --url http://localhost:3000/

# Скопируй URL (например: https://unevenly-subtle-saury.cloudpub.ru:443)
# Установи webhook:
curl -X POST http://localhost:3000/setup-webhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://unevenly-subtle-saury.cloudpub.ru:443/webhook"}'

# Или используй скрипт:
./scripts/setup-webhook.sh https://unevenly-subtle-saury.cloudpub.ru:443/webhook
```

#### Проверка webhook

```bash
# Проверь health check
curl http://localhost:3000/health

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

## 📱 Использование бота

После настройки webhook, найди бота в MAX и отправь команду:

```
/start
```

### Основные команды:

- `/start` или `/help` - главное меню с кнопками
- `/tasks` - список задач
- `/task [название]` - создать задачу
- `/complete [id]` - завершить задачу
- `/pomodoro` - запустить Pomodoro таймер (25 минут)
- `/pomodoro stop` - остановить таймер
- `/pomodoro status` - статус активного таймера
- `/habits` - список привычек с ID
- `/habit [название]` - добавить привычку
- `/mark [id или название]` - отметить выполнение привычки (работает и по ID, и по названию)
- `/career` - карьерные цели
- `/goal [название]` - создать карьерную цель
- `/stats` - статистика (задачи, Pomodoro, привычки, XP, уровень)
- `/progress` - цели пользователя

### Кнопки меню

Бот поддерживает интерактивные кнопки:
- 📝 Задачи - открыть список задач
- 🍅 Pomodoro - запустить таймер
- ✅ Привычки - список привычек
- 📊 Статистика - показать статистику

## 🧪 Тестирование

```bash
# Запуск тестов
npm test

# Тесты в watch режиме
npm run test:watch

# Покрытие кода
npm test -- --coverage
```

### Структура тестов

- `tests/messageHandler.test.js` - тесты обработчика сообщений
- Тесты используют Jest и моки для сервисов

### Ручное тестирование

1. **Локальное тестирование:**
   ```bash
   # Отправь тестовый запрос
   curl -X POST http://localhost:3000/webhook \
     -H "Content-Type: application/json" \
     -d '{
       "message": {
         "sender": {"user_id": 123},
         "body": {"text": "/start"},
         "recipient": {"chat_id": 1, "chat_type": "dialog"}
       },
       "update_type": "message_created"
     }'
   ```

2. **Тестирование в MAX:**
   - Отправь команды боту
   - Проверь логи: `docker compose logs -f bot`
   - Убедись, что все команды работают

## 📦 Структура проекта

```
MAX/
├── src/
│   ├── config/
│   │   └── database.js      # Конфигурация БД
│   ├── services/
│   │   ├── maxApi.js         # MAX API клиент
│   │   ├── userService.js    # Работа с пользователями
│   │   ├── taskService.js    # Управление задачами
│   │   ├── pomodoroService.js # Pomodoro таймер
│   │   ├── habitService.js   # Трекер привычек
│   │   └── careerService.js  # Карьерные цели
│   ├── handlers/
│   │   └── messageHandler.js # Обработка сообщений
│   └── server.js             # Главный сервер
├── Dockerfile
├── docker-compose.yml
├── package.json
├── .env.example
└── README.md
```

## 🔧 Разработка

### Добавление новой команды

1. Добавь обработку в `src/handlers/messageHandler.js`
2. Создай соответствующий сервис в `src/services/` (если нужен)
3. Обнови документацию

### Работа с БД

Все запросы к БД идут через пул соединений в `src/config/database.js`. Таблицы создаются автоматически при первом запуске.

## 🐛 Отладка

```bash
# Логи Docker контейнера
docker-compose logs -f bot

# Логи БД
docker-compose logs -f postgres

# Подключение к БД
docker-compose exec postgres psql -U prodmax -d prodmax
```

## 📊 Мониторинг

- Health check: `GET /health`
- Webhook endpoint: `POST /webhook`
- Установка webhook: `POST /setup-webhook`

## 🔒 Безопасность

- Все запросы к MAX API используют Bearer токен
- БД использует параметризованные запросы (защита от SQL injection)
- Переменные окружения для секретов
- Docker контейнер работает от непривилегированного пользователя

## 🐛 Решение проблем

### Бот не отвечает

1. **Проверь webhook:**
   ```bash
   curl -X GET https://platform-api.max.ru/subscriptions \
     -H "Authorization: YOUR_TOKEN"
   ```

2. **Проверь логи:**
   ```bash
   docker compose logs -f bot
   ```

3. **Проверь health check:**
   ```bash
   curl http://localhost:3000/health
   ```


### Webhook не работает

- Убедись, что туннель (clo/ngrok) запущен
- URL должен быть HTTPS
- Проверь, что URL доступен из интернета


## 👥 Команда

Hydrix | Hackathon Team 607 - VK Education Hackathon 2025

## 🙏 Благодарности

- VK Education за возможность участия
- MAX за отличный API и документацию
- Всем, кто тестировал бота!

---


